"use strict";
var aNumber = maybe;
if (maybe === true) {
    var aBoolean = maybe;
    // const aString: string = maybe // type error
}
if (typeof maybe === 'string') {
    var aString = maybe;
    // const aBoolean: boolean = maybe // type error
}
